# Data

This folder is for small demo data and label examples.

Do not commit raw private Canvas announcements. Use anonymized data only.

Suggested files:

```text
sample_announcements.csv
labeled_announcements.csv
```

Suggested columns:

```text
announcement_id,title,body_text,announcement_type,labels_json
```

