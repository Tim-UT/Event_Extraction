# Third-party notice

The span tagger is fine-tuned from `distilbert-base-uncased`, distributed under the Apache License 2.0. The license text is included as `APACHE-2.0.txt`.

The remaining Python dependencies are listed in `requirements.txt` and retain their respective licenses.
